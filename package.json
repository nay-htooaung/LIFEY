{
  "name": "agent-workflow-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Build pipeline for project management documentation site",
  "scripts": {
    "build": "node scripts/build-docs.js",
    "build:watch": "node scripts/build-docs.js --watch",
    "validate": "node scripts/build-docs.js --validate-only"
  },
  "dependencies": {
    "chokidar": "^4.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^14.0.0"
  }
}
