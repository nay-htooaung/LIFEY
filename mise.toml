[tools]
python = "3.13"
node = "22"
pnpm = "10"
java = "corretto-17"

[settings]
experimental = true

[env]
_.path = ["{{config_root}}/node_modules/.bin"]
ANDROID_HOME = "{{env.LOCALAPPDATA}}/Android/Sdk"
ANDROID_SDK_ROOT = "{{env.LOCALAPPDATA}}/Android/Sdk"
JAVA_HOME = "{{config_root}}/.mise/installs/java/corretto-17.0.19.10.1"

[deps.pnpm]
auto = true

[tasks.build-doc]
description = "Parse .md artifacts → validate → generate docs/dist/index.html"
run = "pnpm run build-doc"

[tasks.build-doc-watch]
description = "Rebuild on file changes"
run = "pnpm run build-doc:watch"

[tasks.validate]
description = "Parse + validate only (no HTML output); exits 1 on errors"
run = "pnpm run validate"

[tasks.convert]
description = "Convert Client Discovery Brief .md templates to .docx and .pdf"
run = "pnpm run convert"
